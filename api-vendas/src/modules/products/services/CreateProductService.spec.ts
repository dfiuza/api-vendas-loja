describe('CreateCustomer', () => {
  it('Should be able to create a new custumer', () => {
    expect(1).toBe(1);
  });

  //Não posso criar conta com mesmo email
  it('Should not be able to create two custumers with the same email', () => {
    expect(1).toBe(1);
  });
});
